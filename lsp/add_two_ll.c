#include <stdio.h>
#include <stdtype.h>

typedef struct node{int val; struct node *next}node_t;
node_t *add_two_ll(node_t *first, node_t *second);


node_t *add_two_ll(node_t *first, node_t *second, uint8_t len){
	node_t *add_ptr = (node_t *)malloc();
}
