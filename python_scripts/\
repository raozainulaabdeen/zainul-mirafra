import re

def find_pattern(txt):
    pattern = '^a(b*)$'
    if re.search(pattern, txt):
        print("pattern is found")
    else:
        print("pattern is not found")


find_pattern("abc")
find_pattern("a")
find_pattern("abba")




